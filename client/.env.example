# VITE_API_URL is auto-detected dynamically for localhost and campus Wi-Fi!
# (Only uncomment and set this if you deploy to Cloud or compile a native Android APK)
# VITE_API_URL=http://localhost:5000/api
